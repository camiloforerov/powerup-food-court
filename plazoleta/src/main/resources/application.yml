server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/plazoleta_db
    username: root
    password: ${db_password}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Plazoleta"
appVersion: "1.0.0"
